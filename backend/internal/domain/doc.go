// Package domain holds core business entities and the repository
// interfaces that describe how they're persisted. It has no dependencies
// on handler, usecase, or repository — everything else depends on it,
// it depends on nothing else in this module.
package domain
