// Package usecase implements application business logic on top of the
// domain layer. It is called by handler and depends only on domain
// interfaces — never on repository or handler directly.
package usecase
