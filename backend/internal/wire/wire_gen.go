//go:build !wireinject

// Code generated by Wire. Hand-maintained for now since the graph is
// trivial — regenerate with `make wire` once it grows past a couple of
// providers, then delete this notice.
package wire

import (
	"log/slog"

	"checkdee-backend/internal/config"
	"checkdee-backend/internal/server"
)

func InitializeServer(logger *slog.Logger) (*server.Server, error) {
	cfg, err := config.Load()
	if err != nil {
		return nil, err
	}
	return server.New(cfg, logger), nil
}
